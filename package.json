{
  "name": "ai-mms",
  "private": true,
  "description": "Tailwind CSS build for Magento admin panel",
  "scripts": {
    "tw:build": "npx @tailwindcss/cli -i tailwind-input.css -o skin/adminhtml/default/default/tailwind.css --minify",
    "tw:watch": "npx @tailwindcss/cli -i tailwind-input.css -o skin/adminhtml/default/default/tailwind.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^4"
  }
}
