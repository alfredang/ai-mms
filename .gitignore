# Environment secrets
.env

# Node tooling — the admin theme is pure PHP + plain CSS; no node build.
node_modules/

# Database dumps
*.sql
*.sql.gz
*.sql.gz.zip

# Country bootstrap seed is intentionally tracked
!seed/country-base.sql.gz

# Site backups
*.zip
*.tar.gz

# Precompressed static asset siblings (generated at Docker build time)
skin/**/*.br
skin/**/*.gz
js/**/*.br
js/**/*.gz

# Media cache (auto-generated thumbnails, regenerated by Magento)
/media/catalog/product/cache/
/media/tmp/
/media/captcha/
/media/css/
/media/js/
/media/migrations-status.json

# Banner source art — multi-MB PNG originals. Compressed and uploaded to R2
# (catalog/category/*.jpg), referenced by URL, so the originals never need to
# ship in the repo or the Docker build context.
/media/banner/

# Var directory (cache, sessions, logs, reports)
/var/*
!/var/.htaccess

# Composer vendor
/vendor/

# Docker volumes
mysql_data/
postgres_data/

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS files
.DS_Store
Thumbs.db

# Lock files
*.LCK

# Playwright/screenshots
.playwright-mcp/
localhost-*.png

# Backup files
*-bkp-*
*.bkp
*_backup_*
*_clone.*
*_Original/
*_OLD/
*_old/
*_BACKUP/

# PHP info / test / disabled files
info.php*
*.phpdisabled

# Magento downloader
/tertiarydownloader/

# Test directories
/test/
/testing/

# Backup local Mage overrides
/app/code/local/Mage_BACKUP/
/app/code/local/Mage/

# Environment-specific config (contains credentials)
/app/etc/local.xml
/app/etc/google-drive-key.json
/docker-compose.override.yml

# Composer cache (composer.lock is tracked for reproducible builds)

# SSL/hosting/cPanel files
.well-known/
.ftpquota
.user.ini
/cgi-bin/

# Legacy/hosting directories
/tertiarycourses.sg/
/malaysia/
/includes/
/shell/
/pdf/

# Sub-migrations with SQL filename exception (override *.sql rule)
!migrations/*.sql

# Skills CLI artifacts (other editors)
.agents/
.trae/
.windsurf/
skills-lock.json

# Local backup snapshots
.site_courses_*/
site_courses_*/

# Hitpay CSS (bundled with the extension, not committed)
skin/frontend/base/default/hitpay/css/

# Windows null-device artifact
/nul

# Magento backup files — never push (large, may contain credentials/PII)
.magento/

# Files claude should ignore to speed up processing
.claudeignore
compose.coolify.local-test.yml
.env.gh

# Source material the project was derived from — not ours to republish.
**/reference/
