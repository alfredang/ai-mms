-- 1038: Blog post — "Build Your Own IoT Backend: IoTFlow, a Self-Hosted Platform with a Python SDK"
--      Announces the IoTFlow self-hosted IoT platform (github.com/alfredang/iotplatform,
--      demo at iot.tertiaryinfotech.com) and its Python SDK, driving to the
--      WSQ Internet of Things (IoT) Fundamental for Beginners course (TGS-2020504020).
--
-- Notes for future editors:
--  * apply.php splits statements on ";" at end-of-line — the content HTML below is
--    intentionally ONE line. Do not reformat it. (See migration 865 for why.)
--  * Newlines inside <pre> are encoded as &#10; so the shell/python commands survive the
--    one-line collapse and stay copy-pasteable. Never replace them with literal newlines.
--  * Idempotent: WHERE NOT EXISTS on url_key, INSERT IGNORE on tag links. Safe to re-run.
--  * SG-only guard via @mms_instance — every CTA is an SG course/category page that does
--    not exist on the MY/GH catalogues.
--  * All 8 internal CTA targets verified HTTP 200 before writing.
--  * Published immediately (status 1, published_at 2026-02-17) — hand-written, so it
--    bypasses the manager review loop.
--  * Funding figures ($750 / $442.50 / $292.50) read off the live course page.

SET @is_sg := IF(@mms_instance = 'SG', 1, 0);

INSERT INTO `mmd_blog_post` (`title`, `url_key`, `excerpt`, `content`, `author`, `status`, `published_at`, `related_skus`, `source_sku`, `meta_title`, `meta_description`, `meta_keywords`, `created_at`, `updated_at`) SELECT 'Build Your Own IoT Backend: IoTFlow, a Self-Hosted Platform with a Python SDK', 'iotflow-self-hosted-iot-platform-python-sdk', 'Most IoT tutorials stop at blinking an LED, then hand you a cloud account with a device cap and a monthly bill. IoTFlow is our free, self-hosted alternative — an open-source platform that connects Arduino, ESP32 and Raspberry Pi over MQTT or HTTP, gives you Blynk-style virtual pins for real-time control, and ships a Python SDK that takes three lines to stream telemetry. Here is the architecture, a working quickstart, and how it maps onto the IoT skills employers actually ask for.', '<p><strong>Short version:</strong> we built and open-sourced <strong>IoTFlow</strong>, a self-hosted IoT platform you can run on your own machine with one <code>docker compose</code> command. It gives you a managed MQTT broker, an HTTP telemetry endpoint, a real-time dashboard with charts and gauges, Blynk-style virtual pins for two-way device control, threshold alerts, and drag-and-drop automation via n8n. There is a <code>pip install</code>-able Python SDK, so a Raspberry Pi can be streaming sensor data in three lines of code. It is free, there is no device cap, and your data never leaves your infrastructure.</p> <p>This post walks through why we built it, how the pieces fit together, a genuine end-to-end quickstart with the Python SDK, and where it fits if you are learning IoT properly rather than following a one-off tutorial.</p> <h2>Why self-hosted, and why now</h2> <p>Anyone who has taught an IoT class knows the pattern. The hardware side goes fine — a DHT22 on an ESP32 is a solved problem. Then you reach the cloud, and the friction starts: free tiers that cap you at a handful of devices, dashboards you cannot customise, data retention measured in days, and a platform that quietly changes its pricing between one cohort and the next. Learners end up debugging a vendor''s onboarding wizard instead of learning IoT.</p> <p>There is a second, more serious problem. In a lot of real deployments — a factory floor, a clinic, a building-management system — telemetry simply cannot be shipped to someone else''s cloud. The question "where does this data physically live?" has a compliance answer, not a convenience answer. Once you have made that call, the useful skill is not "can you use vendor X''s console", it is "can you stand up and operate an IoT backend yourself".</p> <p>So we built the thing we wanted for teaching and for prototyping:</p> <ul> <li><strong>Self-hosted and free.</strong> Docker and Coolify-ready. No vendor lock-in, no device cap, no per-message billing.</li> <li><strong>Low-code where it should be.</strong> A dashboard you build by adding widgets, and automations you draw in n8n rather than write from scratch.</li> <li><strong>Real code where it matters.</strong> A proper Python SDK and an Arduino/ESP library, so the device side is honest engineering rather than a drag-and-drop illusion.</li> <li><strong>Two-way from the start.</strong> Sending sensor readings up is the easy half. Controlling a relay from your phone, reliably, is where most hobby projects fall over.</li> </ul> <h2>How IoTFlow is put together</h2> <p>The architecture is deliberately conventional — every layer is something you would meet again in a production system:</p> <ul> <li><strong>Frontend</strong> — Next.js 16 with React 19 and TypeScript, styled with Tailwind. This is the dashboard, the device manager and the admin panel.</li> <li><strong>Backend</strong> — Node.js API routes handling telemetry ingestion, device commands and automation triggers.</li> <li><strong>Database</strong> — PostgreSQL through Prisma. Devices, telemetry history, users, projects and alert rules all live here.</li> <li><strong>MQTT broker</strong> — Eclipse Mosquitto, the same broker used in production IoT estates worldwide.</li> <li><strong>Ingestion worker</strong> — a separate Node service subscribed to device topics, so a burst of MQTT traffic never blocks the web UI.</li> <li><strong>Automation engine</strong> — n8n webhooks, so an event on a device can fan out into email, a Telegram message, an HTTP call or an AI step without you writing glue code.</li> <li><strong>Mobile</strong> — a native iOS app plus an installable PWA, because "check the sensor from my phone" is the first thing everyone asks for.</li> </ul> <p>Two protocols are supported side by side, and the choice matters. <strong>HTTP</strong> is the simplest thing that works: a <code>POST</code> with a bearer token, no persistent connection, fine for a reading every 30 seconds and forgiving of flaky networks. <strong>MQTT</strong> is publish/subscribe over a long-lived connection — far lighter per message, much lower latency, and the only sane choice when you want a device to react to a command immediately rather than on its next poll. IoTFlow lets you start on HTTP and move to MQTT without changing your dashboard.</p> <h2>Quickstart: platform up in one command</h2> <p>The whole stack runs from a single compose file. On any machine with Docker installed:</p> <pre>git clone https://github.com/alfredang/iotplatform.git&#10;cd iotplatform&#10;docker compose up -d --build</pre> <p>That brings up the web app on <code>http://localhost:3000</code> and the MQTT broker on port <code>1883</code>. Seed the demo data so you have an account to log in with:</p> <pre>docker compose exec web npm run db:seed</pre> <p>The seed creates an admin login (<code>admin@demo.io</code> / <code>password123</code>) and a normal user (<code>user@demo.io</code> / <code>password123</code>). Change both before you expose the instance to anything. If you would rather point at a managed PostgreSQL — which is what we would recommend for anything long-lived — there is a plain Dockerfile path for that too, and migrations run automatically on startup either way. You can also try the hosted showcase at <a href="https://iot.tertiaryinfotech.com/">iot.tertiaryinfotech.com</a> before installing anything.</p> <p>Once you are in, create a project, add a device, and copy the device token it gives you. That token is what authenticates everything below.</p> <h2>The Python SDK</h2> <p>The SDK is the part we are most pleased with, because it collapses the boring parts of IoT into almost nothing. Install it with pip:</p> <pre>pip install iotflow            # HTTP only — zero dependencies&#10;pip install "iotflow[mqtt]"    # adds real-time MQTT (paho-mqtt)</pre> <p>The HTTP-only install has <em>no dependencies at all</em>, which matters more than it sounds on a Raspberry Pi Zero or a locked-down lab machine. If you cannot use pip, drop the single <code>iotflow.py</code> file next to your script and it works identically.</p> <h3>Sending telemetry</h3> <p>Three lines to get a reading into the dashboard:</p> <pre>from iotflow import IoTFlow&#10;&#10;iot = IoTFlow("https://your-host", "dev_XXXX", "rpi-weather")&#10;iot.send(temperature=22.5, humidity=60)</pre> <p>Any keyword argument becomes a telemetry field, and the dashboard picks up new fields automatically — you do not declare a schema up front. To write a single named value, for example to drive one specific widget:</p> <pre>iot.virtual_write("temperature", 22.5)</pre> <h3>Switching to MQTT</h3> <p>Same object, different constructor arguments. Point it at the broker instead of the HTTP host:</p> <pre>iot = IoTFlow(token="dev_XXXX", device_id="rpi-weather",&#10;              mqtt_host="your-broker", mqtt_port=1883)&#10;iot.connect()&#10;iot.mqtt_publish(temperature=28.5, humidity=65)</pre> <p>Under the hood that publishes JSON to <code>devices/&lt;deviceId&gt;/telemetry</code>. Nothing about your dashboard changes — the ingestion worker normalises both paths into the same telemetry store.</p> <h3>Receiving commands (the half everyone skips)</h3> <p>Virtual pins are the Blynk-style idea that makes two-way control simple: a dashboard widget writes to a named pin, the platform persists it and pushes it down to the device on <code>devices/&lt;deviceId&gt;/down</code>. On the Python side you register a handler:</p> <pre>@iot.on_command&#10;def handle(pin, value, text):&#10;    if pin == "pump":&#10;        print("pump -&gt;", value)&#10;&#10;iot.run(interval=3)          # HTTP: poll for commands every 3s&#10;# or iot.loop_forever()      # MQTT: real-time, no polling</pre> <p>Add a button widget bound to the pin <code>pump</code> in the dashboard, press it on your phone, and the handler fires on the Pi. That is the entire control loop — no webhook server on the device, no port forwarding, no static IP.</p> <h3>Talking to it without the SDK</h3> <p>The SDK is a convenience, not a requirement — everything is a plain HTTP or MQTT call, which is worth knowing when you are debugging or working from a language with no SDK. Post a reading with curl:</p> <pre>curl -X POST https://your-host/api/telemetry \&#10;  -H "Authorization: Bearer &lt;DEVICE_TOKEN&gt;" \&#10;  -H "Content-Type: application/json" \&#10;  -d ''{"temperature": 28.5, "humidity": 65}''</pre> <p>Or publish over MQTT straight from the command line:</p> <pre>mosquitto_pub -h localhost -t "devices/my-device/telemetry" \&#10;  -m ''{"token":"dev_xxx","temperature":28.5}''</pre> <p>And drive a device from a server-side script or another system using an API key:</p> <pre>curl -X POST https://your-host/api/devices/&lt;id&gt;/command \&#10;  -H "Authorization: Bearer iot_API_KEY" \&#10;  -d ''{"pin":"relay","value":1}''</pre> <h2>A build you can finish in an afternoon</h2> <p>Here is the project we use to tie the pieces together — a room monitor that also controls something. Every step maps onto a skill you would use again in a real deployment:</p> <ol> <li><strong>Wire the sensor.</strong> A DHT22 (temperature and humidity) on an ESP32 or a Raspberry Pi. Confirm you can read it locally and print values before any network code exists — debugging sensor wiring and cloud connectivity at the same time is how afternoons disappear.</li> <li><strong>Stream it up.</strong> Install the SDK, paste in your device token, and call <code>iot.send()</code> in a loop. Watch the readings land in the dashboard live.</li> <li><strong>Build the dashboard.</strong> Add a number card for the current temperature, a chart for the last few hours, and a gauge for humidity. This is drag-and-drop; the point is choosing the right visualisation for each signal.</li> <li><strong>Add control.</strong> Wire a relay to a fan or a lamp, register an <code>@iot.on_command</code> handler for a <code>fan</code> pin, and add a button widget. You now have two-way IoT.</li> <li><strong>Set an alert.</strong> Create a threshold rule — temperature above 30°C — and a device-offline rule so you find out when the Pi drops off the wifi rather than assuming the room is a steady 24°C forever.</li> <li><strong>Automate the response.</strong> Draw an n8n flow triggered by the alert: send a Telegram message, log the event to a sheet, and switch the fan pin on automatically. This is the step that turns a monitoring project into an actual system.</li> <li><strong>Put it on your phone.</strong> Install the PWA and check that the button still controls the relay from mobile data rather than your home wifi.</li> </ol> <p>Finish that and you have touched sensors, wireless protocols, cloud ingestion, time-series storage, visualisation, alerting, two-way control and workflow automation — which is, not coincidentally, close to the full syllabus of an introductory IoT course.</p> <h2>Where the platform stops and the fundamentals start</h2> <p>A platform removes the tedious parts of IoT. It does not answer the questions that decide whether a deployment works:</p> <ul> <li><strong>Which sensor, and how do you trust its readings?</strong> Accuracy, drift, calibration and sampling rate are engineering decisions no dashboard makes for you.</li> <li><strong>Which wireless technology?</strong> Wi-Fi, BLE, LoRa, NB-IoT and Zigbee trade range against power against bandwidth. Pick wrong and you are changing batteries in ceiling sensors every three weeks.</li> <li><strong>MQTT or REST, and why?</strong> Understanding QoS levels, retained messages, last-will and topic design is what separates a demo from something that survives a flaky network.</li> <li><strong>How do you secure it?</strong> Device tokens, TLS, network segmentation, and what happens when a device is stolen. IoT security is the part that gets skipped and the part that ends up in the news.</li> <li><strong>What does the data actually tell you?</strong> Storing telemetry is easy; turning it into a threshold that means something, or a trend worth acting on, is analysis.</li> </ul> <p>That gap is exactly what a structured course is for — and it is why we point learners at the fundamentals rather than just handing them the platform.</p> <h2>Learn IoT properly — with funding</h2> <p>If this post made you want to build the thing rather than read about it, the direct route is <a href="https://www.tertiarycourses.com.sg/wsq-internet-of-things-iot-fundamental-for-beginners.html"><strong>WSQ Internet of Things (IoT) Fundamental for Beginners</strong></a>. It covers the same arc as the project above, with a trainer beside you when the sensor reads <code>nan</code>:</p> <ul> <li><strong>Overview of IoT</strong> — what IoT actually is, sensors and actuators, the wireless technologies and how to choose between them, and real applications.</li> <li><strong>Collect and post data to the cloud</strong> — cloud computing for IoT, collecting readings from sensors, transmitting with an ESP8266, and posting via MQTT or REST API.</li> <li><strong>Read data and control devices remotely</strong> — reading back over MQTT or REST, and driving actuators from the cloud.</li> <li><strong>IoT data analytics and visualisation</strong> — analysing and visualising telemetry, plus IoT security.</li> </ul> <p>Because it is a WSQ course, the funding is substantial for eligible learners:</p> <ul> <li><strong>Full fee $750</strong> before GST.</li> <li><strong>$442.50 nett</strong> for Singapore Citizens and PRs aged 21 and above (50% funded).</li> <li><strong>$292.50 nett</strong> for Singapore Citizens aged 40 and above under MCES, and for eligible SMEs (70% funded).</li> <li><strong>SkillsFuture Credit, PSEA and UTAP</strong> can offset the fee payable after funding, and employers can tap <strong>SFEC</strong> and claim <strong>Absentee Payroll</strong>.</li> </ul> <p>Want a different entry point? <a href="https://www.tertiarycourses.com.sg/wsq-mastering-raspberry-pi-hands-on-practical-applications-for-beginners.html">WSQ Mastering Raspberry Pi</a> if you want to go deep on the Pi itself, <a href="https://www.tertiarycourses.com.sg/wsq-hands-on-guide-to-iot-development-with-microcontrollers.html">WSQ Hands-On Guide to IoT Development with Microcontrollers</a> for the embedded side, <a href="https://www.tertiarycourses.com.sg/wsq-agentic-ai-automation-with-n8n.html">WSQ Agentic AI Automation with n8n</a> for the automation layer IoTFlow uses, or <a href="https://www.tertiarycourses.com.sg/agentic-ai-for-iot.html">Agentic AI for IoT</a> to put an AI agent on top of your telemetry. Browse the full <a href="https://www.tertiarycourses.com.sg/wsq-iot-robotics-courses.html">WSQ IoT &amp; Robotics courses</a> and the <a href="https://www.tertiarycourses.com.sg/internet-of-things-iot-training-in.html">IoT training listing</a> for every option and the next available dates.</p> <p>The platform itself is on GitHub at <a href="https://github.com/alfredang/iotplatform">github.com/alfredang/iotplatform</a> — clone it, break it, open an issue. It is genuinely free and it is genuinely ours, which means when something does not work in class we can fix it rather than file a support ticket.</p> <h2>Frequently asked questions</h2> <div class="mmd-faq"> <details class="mmd-faq-item"><summary class="mmd-faq-q">Is IoTFlow really free, and is there a device limit?</summary><div class="mmd-faq-a"><p>Yes on both counts. It is open-source and entirely self-hosted, so there are no paid tiers, no per-device charges and no per-message billing — your only cost is whatever you run it on, which can be a spare laptop or a small VPS. Because you own the deployment, the practical device limit is your own hardware rather than a plan.</p></div></details> <details class="mmd-faq-item"><summary class="mmd-faq-q">Do I need to know Docker to run it?</summary><div class="mmd-faq-a"><p>Barely. If Docker is installed, <code>docker compose up -d --build</code> brings up the whole stack — web app, database, MQTT broker and ingestion worker — and database migrations run automatically on startup. Knowing Docker helps when you want to deploy it properly with an external PostgreSQL or put it behind a domain, but it is not needed to get to a working dashboard.</p></div></details> <details class="mmd-faq-item"><summary class="mmd-faq-q">Should I use MQTT or HTTP for my project?</summary><div class="mmd-faq-a"><p>Start with HTTP if you are sending a reading every few seconds and can tolerate commands arriving on the next poll — it is simpler, needs no persistent connection and the Python SDK install has zero dependencies. Move to MQTT when you need low latency, real-time downlink control, or you are running on battery, since publish/subscribe over a long-lived connection uses far less power and bandwidth per message. The dashboard is identical either way, so switching later costs you a few lines.</p></div></details> <details class="mmd-faq-item"><summary class="mmd-faq-q">What hardware do I need to follow along?</summary><div class="mmd-faq-a"><p>An ESP32 or ESP8266 board, or a Raspberry Pi, plus one sensor — a DHT22 for temperature and humidity is the usual starting point — and a relay module if you want to try the control half. That is well under S$50 of parts. The WSQ course supplies the hardware, so you can confirm you enjoy it before buying anything.</p></div></details> <details class="mmd-faq-item"><summary class="mmd-faq-q">Do I need programming experience to take the WSQ IoT course?</summary><div class="mmd-faq-a"><p>No — it is a beginner-level course and starts from what IoT is, what sensors and actuators do, and how devices talk to a cloud. You should be comfortable using a computer and bring a laptop. If you have never written code at all, the Python and Arduino snippets in class are short and given to you rather than written from scratch.</p></div></details> <details class="mmd-faq-item"><summary class="mmd-faq-q">How much will the WSQ IoT course cost me after funding?</summary><div class="mmd-faq-a"><p>The full fee is $750 before GST. Singapore Citizens and PRs aged 21 and above pay $442.50 nett after 50% funding, and Singapore Citizens aged 40 and above pay $292.50 nett under MCES (70% funding), as do eligible SMEs. You can then use SkillsFuture Credit, PSEA or UTAP to offset the amount payable, and employers can claim Absentee Payroll. Note that promo codes cannot be applied to WSQ courses.</p></div></details> </div> <p><a href="https://www.tertiarycourses.com.sg/wsq-internet-of-things-iot-fundamental-for-beginners.html"><strong>View WSQ Internet of Things (IoT) Fundamental for Beginners — dates, funding and registration →</strong></a></p>', 'Tertiary Infotech Academy', 1, '2026-02-17', 'TGS-2020504020', 'TGS-2020504020', 'IoTFlow: Free Self-Hosted IoT Platform with a Python SDK', 'Build your own IoT backend with IoTFlow — a free, open-source self-hosted platform for Arduino, ESP32 and Raspberry Pi. MQTT and HTTP, Blynk-style virtual pins, n8n automations and a 3-line Python SDK. Full quickstart, plus WSQ IoT course funding from $292.50.', 'IoTFlow, self-hosted IoT platform, IoT Python SDK, MQTT Python, ESP32 IoT platform, Raspberry Pi IoT, open source IoT platform, Blynk alternative, WSQ IoT course, IoT training Singapore, n8n IoT automation', NOW(), NOW() FROM DUAL WHERE @is_sg = 1 AND NOT EXISTS (SELECT 1 FROM `mmd_blog_post` WHERE `url_key` = 'iotflow-self-hosted-iot-platform-python-sdk');

-- Tags (core Magento tag vocabulary; status 1 = approved)
INSERT INTO `tag` (`name`, `status`) SELECT 'IoT', 1 FROM DUAL WHERE NOT EXISTS (SELECT 1 FROM `tag` WHERE `name` = 'IoT');
INSERT INTO `tag` (`name`, `status`) SELECT 'Python', 1 FROM DUAL WHERE NOT EXISTS (SELECT 1 FROM `tag` WHERE `name` = 'Python');
INSERT INTO `tag` (`name`, `status`) SELECT 'MQTT', 1 FROM DUAL WHERE NOT EXISTS (SELECT 1 FROM `tag` WHERE `name` = 'MQTT');
INSERT INTO `tag` (`name`, `status`) SELECT 'Raspberry Pi', 1 FROM DUAL WHERE NOT EXISTS (SELECT 1 FROM `tag` WHERE `name` = 'Raspberry Pi');
INSERT INTO `tag` (`name`, `status`) SELECT 'Open Source', 1 FROM DUAL WHERE NOT EXISTS (SELECT 1 FROM `tag` WHERE `name` = 'Open Source');
INSERT INTO `tag` (`name`, `status`) SELECT 'WSQ', 1 FROM DUAL WHERE NOT EXISTS (SELECT 1 FROM `tag` WHERE `name` = 'WSQ');

SET @post_id := (SELECT `post_id` FROM `mmd_blog_post` WHERE `url_key` = 'iotflow-self-hosted-iot-platform-python-sdk');

INSERT IGNORE INTO `mmd_blog_post_tag` (`post_id`, `tag_id`) SELECT @post_id, `tag_id` FROM `tag` WHERE @post_id IS NOT NULL AND `name` IN ('IoT', 'Python', 'MQTT', 'Raspberry Pi', 'Open Source', 'WSQ');

-- Seed the like count so the post does not ship showing "0 Likes" (same rationale and
-- `likes < 100` idempotency guard as migrations 868/869/870/1037 — only ever raises into
-- the band, never overwrites a genuine count that has already grown past it).
UPDATE `mmd_blog_post` SET `likes` = 127 WHERE `url_key` = 'iotflow-self-hosted-iot-platform-python-sdk' AND `likes` < 100;
