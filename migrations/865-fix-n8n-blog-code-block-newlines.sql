-- 865: Fix the n8n/Hostinger post's code blocks (migration 864).
--
-- 864 was generated by a script that collapsed the whole article to one line so
-- apply.php's semicolon-at-EOL splitter would keep it as a single statement. That
-- collapse also ate the newlines INSIDE <pre> blocks, so every multi-line shell
-- command rendered as one unbroken line and was not copy-pasteable.
--
-- This re-writes the content with the <pre> newlines encoded as &#10; — preserved
-- through the one-line collapse, and rendered as real line breaks by the browser
-- inside the white-space:pre code block (see skin/frontend/ultimo/default/css/blog.css).
--
-- Idempotent: a plain UPDATE keyed on url_key. Safe to re-run.

UPDATE `mmd_blog_post` SET `content` = '<p>n8n is the workflow automation tool that has quietly become the backbone of serious AI automation work. Unlike Zapier or Make, you can <strong>self-host it</strong> — which means no per-execution pricing, no task limits, and your data never leaves a server you control. The cheapest reliable way to do that is a small VPS.</p> <p>This guide walks through installing n8n on a <a href="https://www.hostinger.com?REFERRALCODE=FEGANGCHQ20C" target="_blank" rel="noopener sponsored">Hostinger VPS</a> from scratch: the one-click template route (5 minutes), the manual Docker route (full control), then HTTPS and the security settings most tutorials leave out.</p> <h2>Why self-host n8n instead of using n8n Cloud?</h2> <table> <tr><th></th><th>n8n Cloud</th><th>Self-hosted on a VPS</th></tr> <tr><td>Cost model</td><td>Monthly fee, capped executions</td><td>Flat VPS cost, unlimited executions</td></tr> <tr><td>Data residency</td><td>n8n''s servers</td><td>Your server, your jurisdiction</td></tr> <tr><td>Credentials &amp; API keys</td><td>Stored by the vendor</td><td>Stored in your own database</td></tr> <tr><td>Community nodes</td><td>Restricted</td><td>Install anything</td></tr> <tr><td>Setup effort</td><td>None</td><td>~15 minutes (this guide)</td></tr> </table> <p>For anyone handling client data, internal company records, or anything covered by PDPA, the data-residency column is usually the deciding one. That is why most corporate automation teams we train end up self-hosting.</p> <h2>What you need before you start</h2> <ul> <li><strong>A VPS.</strong> Minimum 1 vCPU / 1&nbsp;GB RAM (KVM 1). We recommend <strong>2 vCPU / 2&nbsp;GB RAM (KVM 2)</strong> — n8n gets sluggish on 1&nbsp;GB once you run AI nodes or a few concurrent workflows. Grab one here: <a href="https://www.hostinger.com?REFERRALCODE=FEGANGCHQ20C" target="_blank" rel="noopener sponsored"><strong>Hostinger VPS (20% off with our link)</strong></a></li> <li><strong>Ubuntu</strong> as the operating system</li> <li><strong>A domain or subdomain</strong> (e.g. <code>n8n.yourdomain.com</code>) if you want HTTPS — and you do, because webhooks from most services require it</li> <li><strong>SSH access</strong>, or just the hPanel browser terminal</li> </ul> <h2>Method 1: The one-click n8n template (fastest)</h2> <p>Hostinger ships a prebuilt n8n VPS template. If you just want n8n running, use this.</p> <ol> <li>Log in to <strong>hPanel</strong> and open the <strong>VPS</strong> section</li> <li>Click <strong>Manage</strong> on your VPS</li> <li>Open the <strong>OS &amp; Panel</strong> dropdown and choose <strong>Operating System</strong></li> <li>Search for and select the <strong>n8n</strong> template</li> <li>Confirm the OS-overwrite warning — this wipes the server, so only do it on a fresh VPS</li> <li>Set your <strong>root password</strong> and wait for the build to finish</li> <li>Click <strong>Panel access</strong> &rarr; <strong>Login URL</strong></li> <li>Create your <strong>owner account</strong> (email + strong password) and the n8n dashboard loads</li> </ol> <p>That is a working n8n. Skip to <a href="#secure">securing it</a> — do not leave it on the default settings.</p> <h2>Method 2: Manual install with Docker (recommended for production)</h2> <p>Docker is the route we use in our own workshops: it upgrades cleanly, isolates n8n from the host, and keeps your workflows in a mounted volume that survives container rebuilds.</p> <h3>Step 1 — Update the server</h3> <pre><code>sudo apt-get update &amp;&amp; sudo apt-get upgrade -y</code></pre> <h3>Step 2 — Install Docker</h3> <pre><code>sudo apt-get install -y apt-transport-https ca-certificates curl software-properties-common&#10;curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /usr/share/keyrings/docker-archive-keyring.gpg&#10;echo "deb [arch=amd64 signed-by=/usr/share/keyrings/docker-archive-keyring.gpg] https://download.docker.com/linux/ubuntu $(lsb_release -cs) stable" | sudo tee /etc/apt/sources.list.d/docker.list &gt; /dev/null&#10;sudo apt-get update&#10;sudo apt-get install -y docker-ce docker-ce-cli containerd.io&#10;docker --version</code></pre> <h3>Step 3 — Pull and run n8n</h3> <pre><code>docker pull n8nio/n8n&#10;docker run -d --name n8n -p 5678:5678 -v ~/.n8n:/home/node/.n8n n8nio/n8n</code></pre> <p>The <code>-v ~/.n8n:/home/node/.n8n</code> volume mount is the important part — that is where your workflows and credentials live. Without it, rebuilding the container wipes everything.</p> <p>Visit <code>http://your-server-ip:5678</code> and create your owner account.</p> <h3>Step 4 — Fix permissions (if the container will not start)</h3> <p>The single most common failure. If <code>docker logs n8n</code> shows permission errors:</p> <pre><code>sudo chown -R 1000:1000 ~/.n8n&#10;sudo chmod -R 755 ~/.n8n&#10;docker start n8n</code></pre> <h2 id="secure">Step 5 — Secure your instance</h2> <p>An n8n instance on a public IP with no auth is an open door to your connected accounts. Stop the container and relaunch it with authentication and your domain configured:</p> <pre><code>docker stop n8n &amp;&amp; docker rm n8n&#10;docker run -d --name n8n \\&#10;  -p 5678:5678 \\&#10;  -e N8N_BASIC_AUTH_ACTIVE=true \\&#10;  -e N8N_BASIC_AUTH_USER=your_username \\&#10;  -e N8N_BASIC_AUTH_PASSWORD=your_strong_password \\&#10;  -e N8N_HOST=n8n.yourdomain.com \\&#10;  -e N8N_PORT=5678 \\&#10;  -e WEBHOOK_URL=https://n8n.yourdomain.com/ \\&#10;  -e GENERIC_TIMEZONE=Asia/Singapore \\&#10;  -v ~/.n8n:/home/node/.n8n \\&#10;  n8nio/n8n</code></pre> <p>Set <code>GENERIC_TIMEZONE</code> to <code>Asia/Singapore</code> if you are scheduling workflows for Singapore business hours — leaving it on UTC is why so many "my cron fired at the wrong time" questions get posted.</p> <h2>Step 6 — Add HTTPS with a free Let''s Encrypt certificate</h2> <p>Point your subdomain''s DNS <strong>A record</strong> at your VPS IP first, then:</p> <pre><code>sudo apt update &amp;&amp; sudo apt install nginx certbot python3-certbot-nginx -y&#10;sudo systemctl enable nginx&#10;sudo systemctl start nginx&#10;sudo nano /etc/nginx/sites-available/n8n</code></pre> <p>Paste this reverse-proxy configuration:</p> <pre><code>server {&#10;    server_name n8n.yourdomain.com;&#10;    location / {&#10;        proxy_pass http://localhost:5678;&#10;        proxy_set_header Host $host;&#10;        proxy_set_header X-Real-IP $remote_addr;&#10;        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;&#10;        proxy_set_header X-Forwarded-Proto $scheme;&#10;    }&#10;    listen 80;&#10;}</code></pre> <p>Enable the site and issue the certificate:</p> <pre><code>sudo ln -s /etc/nginx/sites-available/n8n /etc/nginx/sites-enabled/&#10;sudo systemctl restart nginx&#10;sudo certbot --nginx -d n8n.yourdomain.com</code></pre> <p>Your instance is now live at <code>https://n8n.yourdomain.com</code>.</p> <h3>Auto-renew the certificate</h3> <p>Let''s Encrypt certificates expire every 90 days. Automate the renewal:</p> <pre><code>sudo crontab -e</code></pre> <p>Add this line:</p> <pre><code>0 2 * * * certbot renew --quiet --post-hook "systemctl restart nginx"</code></pre> <h2>Common problems and fixes</h2> <table> <tr><th>Symptom</th><th>Likely cause</th><th>Fix</th></tr> <tr><td>Container exits immediately</td><td>Volume permissions</td><td><code>sudo chown -R 1000:1000 ~/.n8n</code></td></tr> <tr><td>Webhooks never fire</td><td>No HTTPS, or <code>WEBHOOK_URL</code> not set</td><td>Complete Step 6 and set <code>WEBHOOK_URL</code></td></tr> <tr><td>Workflows vanish after restart</td><td>Missing volume mount</td><td>Always run with <code>-v ~/.n8n:/home/node/.n8n</code></td></tr> <tr><td>Scheduled runs at odd hours</td><td>Timezone left as UTC</td><td>Set <code>GENERIC_TIMEZONE=Asia/Singapore</code></td></tr> <tr><td>Sluggish with AI nodes</td><td>1&nbsp;GB RAM plan</td><td>Upgrade to 2&nbsp;GB (KVM 2) or higher</td></tr> </table> <h2>You have n8n running. Now what?</h2> <p>Installing n8n is the easy part — the value is in what you build with it. The teams getting real returns are the ones building <strong>agentic workflows</strong>: automations that call an LLM, make decisions, use tools, and handle exceptions without a human in the loop.</p> <p>That is exactly what we teach, hands-on, in Singapore:</p> <ul> <li><a href="{{store direct_url=''wsq-agentic-ai-automation-with-n8n.html''}}"><strong>WSQ - Agentic AI Automation with n8n</strong></a> — our flagship n8n course, <strong>WSQ-funded up to 70%</strong> for eligible Singaporeans and PRs, and claimable with SkillsFuture Credit</li> <li><a href="{{store direct_url=''agentic-ai-with-n8n.html''}}"><strong>Agentic AI with n8n</strong></a> — build AI agents that reason and act across your tools</li> <li><a href="{{store direct_url=''build-rag-chatbot-with-n8n.html''}}"><strong>Build a RAG Chatbot with n8n</strong></a> — connect your own documents to a chatbot that answers from them</li> <li><a href="{{store direct_url=''wsq-automate-video-and-voice-ai-agents-with-n8n.html''}}"><strong>WSQ - Automate Video and Voice AI Agents with n8n</strong></a> — voice and video agents, also WSQ-funded</li> </ul> <p>Browse every <a href="{{store direct_url=''wsq-agentic-ai-courses.html''}}"><strong>WSQ agentic AI course</strong></a> to check funding and upcoming dates.</p> <h2>Frequently asked questions</h2> <h3>How much does it cost to self-host n8n?</h3> <p>Just the VPS. A 2&nbsp;vCPU / 2&nbsp;GB plan is a few dollars a month and runs unlimited workflow executions — which is why self-hosting pays for itself quickly compared with per-task pricing. You can start with <a href="https://www.hostinger.com?REFERRALCODE=FEGANGCHQ20C" target="_blank" rel="noopener sponsored">20% off a Hostinger VPS here</a>.</p> <h3>Is self-hosted n8n free?</h3> <p>n8n''s Community Edition is source-available and free to self-host for internal business use. Some enterprise features (SSO, advanced permissions) require a paid licence. For most teams the community edition is more than enough.</p> <h3>Do I need to know Linux?</h3> <p>For Method 1 (the one-click template), no — it is all point-and-click in hPanel. For Method 2 you only need to paste the commands above. Nothing here requires prior Linux experience.</p> <h3>Can I move my workflows from n8n Cloud to my own server?</h3> <p>Yes. Export your workflows as JSON from the cloud instance and import them into the self-hosted one. You will need to re-enter credentials, since those are deliberately not included in exports.</p> <h3>Will my automations keep running if I close my browser?</h3> <p>Yes — with the Docker method the container runs as a background service and survives reboots. (If you used the <code>npm</code> install method with <code>screen</code>, that is far less reliable for production; use Docker.)</p> <h3>Which n8n course should I start with in Singapore?</h3> <p>If you are new to n8n, start with <a href="{{store direct_url=''wsq-agentic-ai-automation-with-n8n.html''}}"><strong>WSQ - Agentic AI Automation with n8n</strong></a> — it covers the fundamentals and moves into agentic workflows, and it is WSQ-funded so most learners pay a fraction of the listed fee.</p> <h2>Next steps</h2> <ol> <li><a href="https://www.hostinger.com?REFERRALCODE=FEGANGCHQ20C" target="_blank" rel="noopener sponsored"><strong>Spin up a Hostinger VPS at 20% off</strong></a> (2&nbsp;GB plan recommended)</li> <li>Follow Method 1 or Method 2 above — you will be running in about 15 minutes</li> <li>Secure it with basic auth and HTTPS (Steps 5 and 6)</li> <li><a href="{{store direct_url=''wsq-agentic-ai-automation-with-n8n.html''}}"><strong>Join our next WSQ n8n class</strong></a> and build agentic automations you can put straight into production</li> </ol> <p><em>Disclosure: the Hostinger links in this article are referral links — using them costs you nothing extra and gets you 20% off, while supporting our free tutorials. Commands and steps were correct at time of writing; check Hostinger''s and n8n''s official documentation for the latest.</em></p>', `updated_at` = NOW() WHERE `url_key` = 'how-to-install-n8n-on-hostinger-vps';
