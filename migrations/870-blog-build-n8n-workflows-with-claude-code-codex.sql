-- 870: Blog post — "Stop Dragging Nodes: Build n8n Workflows with Claude Code or Codex Instead"
--      Building n8n workflows by generating the workflow JSON with an AI coding agent
--      (Claude Code / OpenAI Codex) instead of dragging nodes in the GUI, plus a short
--      Hostinger VPS self-hosting section with the referral link and the how-to video.
--
-- Notes for future editors:
--  * apply.php splits statements on ";" at end-of-line — the content HTML below is
--    intentionally ONE line. Do not reformat it. Newlines inside <pre> are &#10; so they
--    survive the collapse (see migration 865 for why).
--  * Idempotent: WHERE NOT EXISTS on url_key, INSERT IGNORE on tag links. Safe to re-run.
--  * SG-only guard via @mms_instance — the CTAs are SG WSQ/SkillsFuture courses and the two
--    series category pages, none of which exist on the MY/GH catalogues.
--  * All CTA targets verified HTTP 200 on www.tertiarycourses.com.sg before writing.
--  * Published immediately (status 1) — hand-written, so it bypasses the manager review loop.

SET @is_sg := IF(@mms_instance = 'SG', 1, 0);

INSERT INTO `mmd_blog_post` (`title`, `url_key`, `excerpt`, `content`, `author`, `status`, `published_at`, `related_skus`, `meta_title`, `meta_description`, `meta_keywords`, `created_at`, `updated_at`) SELECT 'Stop Dragging Nodes: Build n8n Workflows with Claude Code or Codex Instead', 'build-n8n-workflows-with-claude-code-codex', 'An n8n workflow is just a JSON file. That means you can describe what you want to an AI coding agent like Claude Code or OpenAI Codex, have it generate the whole workflow, and import it in one paste — no dragging, no mis-typed expressions, no silently broken node connections. Here is the exact method, the prompt template, and how to self-host n8n on a cheap Hostinger VPS so you can run it all unlimited.', '<p><strong>Short version:</strong> an n8n workflow is a JSON document, not a drawing. Any AI coding agent that can write JSON can therefore write an entire workflow — nodes, connections, expressions, error branches and all. Describe the automation in plain English to <a href="https://claude.com/claude-code" target="_blank">Claude Code</a> or <a href="https://openai.com/codex/" target="_blank">OpenAI Codex</a>, paste the generated JSON into the n8n canvas with <strong>Ctrl+V</strong>, and a build that takes 40 minutes of dragging takes about three minutes — with far fewer broken expressions.</p> <p>We teach this method in class because it changes what a beginner can ship on day one. Below is the full workflow-as-code method, a prompt template you can copy, the failure modes to watch for, and how to self-host n8n cheaply so you are not paying per execution while you experiment.</p> <h2>Why manual node-dragging is the slow, error-prone path</h2> <p>Building in the n8n GUI feels productive because you can see the workflow forming. But most of the real time goes somewhere else entirely: looking up which node does the thing you want, guessing the exact shape of the data coming out of the previous node, and typing expressions like <code>{{ $json.body.items[0].email }}</code> from memory. Get one character wrong and the node fails at runtime with a message that points at the symptom, not the cause.</p> <table> <tr><th>Task</th><th>Manual GUI build</th><th>Generated with Claude Code / Codex</th></tr> <tr><td>12-node workflow, first draft</td><td>30-60 minutes</td><td>2-5 minutes</td></tr> <tr><td>Expression syntax errors</td><td>Common — typed by hand, found at runtime</td><td>Rare — the model has seen thousands of valid expressions</td></tr> <tr><td>Node discovery</td><td>Manual search through 400+ nodes</td><td>The model proposes the node</td></tr> <tr><td>Error handling branches</td><td>Usually skipped "for now"</td><td>Ask for it once, included every time</td></tr> <tr><td>Documenting the workflow</td><td>Rarely done</td><td>Sticky notes generated inline</td></tr> <tr><td>Version control</td><td>Manual export, if you remember</td><td>The JSON is already in your repo</td></tr> </table> <p>The last row is the one people underestimate. When workflows live as JSON files in a Git repo, you get diffs, code review, rollback and branching for your automations — the things software teams have had for thirty years and automation teams generally have not.</p> <h2>The key insight: an n8n workflow is just JSON</h2> <p>Open any workflow in n8n, press <strong>Ctrl+A</strong> then <strong>Ctrl+C</strong>, and paste into a text editor. You get a JSON object with two parts that matter:</p> <ul> <li><code>nodes</code> — an array where every node has a <code>name</code>, a <code>type</code> (for example <code>n8n-nodes-base.httpRequest</code>), a <code>position</code> as [x, y], and a <code>parameters</code> object holding its settings.</li> <li><code>connections</code> — an object mapping each node name to the nodes its output feeds into.</li> </ul> <p>That is the entire format. And critically, the reverse works too: <strong>copy valid workflow JSON to your clipboard, click on the n8n canvas and press Ctrl+V, and n8n renders it as a live workflow</strong>. This clipboard round-trip is the whole trick. It means any tool that can produce correct JSON is a legitimate n8n workflow builder.</p> <h2>Method 1: Claude Code (best for repo-based, multi-workflow projects)</h2> <p>Claude Code runs in your terminal, reads and writes files in your project, and can iterate on its own output. That makes it the strongest option when you are building a set of related workflows and want them under version control.</p> <ol> <li>Create a project folder and start Claude Code inside it: <pre>mkdir n8n-workflows &amp;&amp; cd n8n-workflows&#10;git init&#10;claude</pre> </li> <li>Export one workflow you already trust from n8n and save it as <code>examples/reference.json</code>. This single example is the highest-leverage thing you can do — it shows the model your n8n version''s exact node types and parameter shapes.</li> <li>Ask for the workflow in plain English (prompt template below). Claude Code writes it to a <code>.json</code> file in the folder.</li> <li>Open the file, select all, copy, then click the n8n canvas and press <strong>Ctrl+V</strong>.</li> <li>Add your credentials in the n8n UI — never in the JSON — and run the workflow.</li> <li>When something misbehaves, paste the n8n error message back to Claude Code and ask it to fix the specific node. It edits the file in place; re-paste and re-test.</li> </ol> <p>Because everything is a file, you can then <code>git commit</code> each working version, and ask Claude Code things like "create a variant of this that reads from Google Sheets instead of Airtable" — which takes seconds instead of a rebuild.</p> <p>If you want to learn this workflow properly — the file conventions, custom commands, and how to drive it for real automation projects — that is exactly what our <a href="https://www.tertiarycourses.com.sg/claude-code-masterclass.html">Claude Code Masterclass</a> covers, and it sits inside the broader <a href="https://www.tertiarycourses.com.sg/claude-ai-series.html">Claude AI Series</a> of hands-on courses. Singaporeans can offset the fee with <strong>SkillsFuture Credit</strong>.</p> <h2>Method 2: OpenAI Codex (best for fast one-off workflows)</h2> <p>Codex works well when you want a single workflow generated quickly, either from the CLI or the web interface. The method is the same, minus the repo scaffolding:</p> <ol> <li>Paste in one exported example workflow as your format reference.</li> <li>Describe the automation you want, including the trigger, each step, and what happens on failure.</li> <li>Ask for <strong>the complete workflow JSON only, no commentary</strong> — so you can copy the whole response straight to the clipboard.</li> <li>Paste onto the n8n canvas, attach credentials, test.</li> </ol> <p>In practice, teams use both: Codex for quick throwaway automations, Claude Code when the workflows become a maintained asset. Our <a href="https://www.tertiarycourses.com.sg/codex-masterclass.html">Codex Masterclass</a> and <a href="https://www.tertiarycourses.com.sg/codex-for-work-automation.html">Codex for Work Automation</a> courses — both part of the <a href="https://www.tertiarycourses.com.sg/codex-ai-series.html">Codex AI Series</a> — teach the CLI, the prompting discipline and the review habits that keep generated code trustworthy.</p> <h2>The prompt template that actually works</h2> <p>Vague prompts produce plausible-looking workflows that fail on the first run. The fix is to specify the trigger, the data shape and the failure path explicitly. Copy this:</p> <pre>You are generating an n8n workflow as JSON.&#10;&#10;FORMAT REFERENCE&#10;Here is an exported workflow from my n8n instance. Match this&#10;structure, node type names and parameter shapes exactly:&#10;&lt;paste your exported example JSON here&gt;&#10;&#10;WORKFLOW TO BUILD&#10;Trigger: [e.g. Webhook, POST, path /new-lead]&#10;Steps:&#10;  1. [e.g. Validate that email and name are present]&#10;  2. [e.g. Look up the company domain via HTTP Request]&#10;  3. [e.g. Append a row to Google Sheets]&#10;  4. [e.g. Send a Slack message to #sales]&#10;On failure: [e.g. route to a Slack alert in #ops with the error]&#10;&#10;RULES&#10;- Do NOT include any credentials, API keys or tokens.&#10;  Leave credential fields empty; I attach them in the UI.&#10;- Use n8n expression syntax for referencing prior node data.&#10;- Add Sticky Note nodes explaining each section.&#10;- Lay nodes out left to right, ~220px apart, so it is readable.&#10;- Output ONLY the workflow JSON. No explanation before or after.</pre> <p>Three details in that template do most of the work. The <strong>format reference</strong> pins the model to your n8n version. The <strong>credentials rule</strong> keeps secrets out of files and chat logs. And <strong>"output ONLY the JSON"</strong> means you can select-all-copy without manually stripping prose — which matters because a stray sentence makes the clipboard paste fail silently.</p> <h2>What still goes wrong (and how to fix it fast)</h2> <table> <tr><th>Symptom</th><th>Cause</th><th>Fix</th></tr> <tr><td>Paste does nothing on the canvas</td><td>The clipboard contains prose around the JSON</td><td>Copy only from the first <code>{</code> to the last <code>}</code></td></tr> <tr><td>"Unknown node type"</td><td>The model invented a node, or your n8n is older than it assumes</td><td>Give it your exported example and your n8n version number</td></tr> <tr><td>Node runs but data is empty</td><td>Expression references a field name that does not exist</td><td>Run the prior node, copy the real output JSON, paste it back to the agent</td></tr> <tr><td>Credentials error</td><td>Credentials are never in the JSON, by design</td><td>Select the credential in each node in the n8n UI</td></tr> <tr><td>Nodes stacked on top of each other</td><td>Positions not specified</td><td>Ask for left-to-right layout ~220px apart</td></tr> </table> <p>The pattern behind every one of these: <strong>the agent cannot see your n8n instance</strong>. Every time you hand it real data — an exported workflow, an actual error message, a genuine sample payload — accuracy jumps. Treat it as a very fast collaborator who is working blind, and feed it eyes.</p> <h2>You need your own n8n to do this properly</h2> <p>Generating workflows means running a lot of test executions, and installing community nodes the model suggests. On n8n Cloud that burns your execution quota and community nodes are restricted. Self-hosting removes both limits — flat monthly cost, unlimited executions, and your credentials sit in your own database rather than a vendor''s. For anyone handling client data under PDPA, that last point is usually the decider.</p> <h2>How to install n8n on a Hostinger VPS (the quick version)</h2> <p>Hostinger is the cheapest reliable route we have found: VPS plans start around a few dollars a month, the servers are fast, and there is a <strong>one-click n8n template</strong> so you do not have to configure Docker yourself. The short path:</p> <ol> <li><a href="https://www.hostinger.com?REFERRALCODE=FEGANGCHQ20C" target="_blank" rel="noopener sponsored">Order a Hostinger VPS</a> — the 2&nbsp;GB plan is the sensible starting point for n8n.</li> <li>In the setup wizard, choose the operating system by application and pick the <strong>n8n template</strong> instead of a bare Ubuntu image.</li> <li>Set a strong root password and let it provision — it takes a few minutes.</li> <li>Point a subdomain (for example <code>n8n.yourdomain.com</code>) at the VPS IP with an <strong>A record</strong>.</li> <li>Open <code>https://your-subdomain</code>, create the owner account, and enable HTTPS — the template provisions a free Let''s Encrypt certificate once the DNS resolves.</li> <li>Turn on basic auth and take a first backup before you put anything real through it.</li> </ol> <p>Prefer to do it manually with Docker? Hostinger''s own <a href="https://www.hostinger.com/ph/tutorials/how-to-install-n8n" target="_blank" rel="noopener">step-by-step n8n installation tutorial</a> walks through both routes, and our earlier guide, <a href="https://www.tertiarycourses.com.sg/blog/how-to-install-n8n-on-hostinger-vps">How to Install n8n on a Hostinger VPS</a>, covers the Docker commands, the Nginx reverse proxy and the security hardening most tutorials skip.</p> <p>Here is the whole setup on video, including HTTPS and backups:</p> <div style="position:relative;padding-bottom:56.25%;height:0;overflow:hidden;margin:24px 0;border-radius:10px;"><iframe src="https://www.youtube-nocookie.com/embed/SxaDYwcLnuA" title="Set Up n8n on Hostinger Safely: One-Click VPS + HTTPS + Backups" style="position:absolute;top:0;left:0;width:100%;height:100%;border:0;" loading="lazy" allow="accelerometer; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share" referrerpolicy="strict-origin-when-cross-origin" allowfullscreen></iframe></div> <p><strong><a href="https://www.hostinger.com?REFERRALCODE=FEGANGCHQ20C" target="_blank" rel="noopener sponsored">Get 20% off your Hostinger VPS with our referral link</a></strong> — it costs you nothing extra and supports these free tutorials.</p> <h2>A sensible working setup</h2> <ol> <li><strong>Self-hosted n8n on a VPS</strong> — your execution engine, unlimited runs.</li> <li><strong>A Git repo of workflow JSON files</strong> — your source of truth, diffable and reviewable.</li> <li><strong>Claude Code or Codex in that repo</strong> — your builder, with one exported workflow as the format reference.</li> <li><strong>Credentials only in the n8n UI</strong> — never in files, never in a chat window.</li> </ol> <p>That is the setup we run in the classroom, and it is what makes it realistic for someone with no automation background to ship a working multi-step workflow on their first day.</p> <h2>Learn this properly, with funding</h2> <p>Reading about it gets you the concept; building three or four real workflows with someone correcting your prompts gets you the skill. Three routes depending on where you are starting:</p> <ul> <li><strong>You want the AI coding agent skill first</strong> — <a href="https://www.tertiarycourses.com.sg/claude-code-masterclass.html">Claude Code Masterclass</a> or <a href="https://www.tertiarycourses.com.sg/codex-masterclass.html">Codex Masterclass</a>. Both are claimable with <strong>SkillsFuture Credit</strong>.</li> <li><strong>You want the automation skill first</strong> — <a href="https://www.tertiarycourses.com.sg/agentic-ai-with-n8n.html">Agentic AI with n8n</a> teaches n8n itself, from triggers to AI agent nodes.</li> <li><strong>You want a funded, certifiable qualification</strong> — <a href="https://www.tertiarycourses.com.sg/wsq-agentic-ai-automation-with-n8n.html">WSQ Agentic AI Automation with n8n</a> and <a href="https://www.tertiarycourses.com.sg/wsq-agentic-ai-applications-with-claude-code.html">WSQ Agentic AI Applications with Claude Code</a> are WSQ courses with <strong>up to 70% SSG funding</strong> for eligible Singaporeans, plus Absentee Payroll for employers.</li> </ul> <p>Browse the full <a href="https://www.tertiarycourses.com.sg/claude-ai-series.html">Claude AI Series</a> and <a href="https://www.tertiarycourses.com.sg/codex-ai-series.html">Codex AI Series</a> to see every course and the next available dates.</p> <h2>Frequently asked questions</h2> <div class="mmd-faq"> <details class="mmd-faq-item"><summary class="mmd-faq-q">Can Claude Code really build a working n8n workflow from a description?</summary><div class="mmd-faq-a"><p>Yes, reliably, provided you give it one exported workflow from your own n8n as a format reference. Without that reference it guesses at node type names and parameter shapes, and you get a workflow that pastes but does not run. With it, a 10-15 node workflow generated on the first attempt usually needs only credential attachment and one or two small corrections.</p></div></details> <details class="mmd-faq-item"><summary class="mmd-faq-q">Is Codex or Claude Code better for n8n workflows?</summary><div class="mmd-faq-a"><p>Codex is quicker for a single throwaway workflow. Claude Code is better when workflows become a maintained asset, because it works inside a project folder — it can read your existing workflow files, edit them in place, and keep everything in Git. Many teams use both. We teach both in the <a href="https://www.tertiarycourses.com.sg/claude-ai-series.html">Claude AI Series</a> and <a href="https://www.tertiarycourses.com.sg/codex-ai-series.html">Codex AI Series</a>.</p></div></details> <details class="mmd-faq-item"><summary class="mmd-faq-q">How do I import the generated JSON into n8n?</summary><div class="mmd-faq-a"><p>Copy the JSON to your clipboard, click anywhere on an open n8n canvas, and press Ctrl+V (Cmd+V on Mac). n8n parses the clipboard and renders the nodes. Alternatively use Workflows &rarr; Import from File. If nothing appears when you paste, your clipboard almost certainly contains explanatory text around the JSON — copy only from the first opening brace to the last closing brace.</p></div></details> <details class="mmd-faq-item"><summary class="mmd-faq-q">Should I put my API keys in the workflow JSON?</summary><div class="mmd-faq-a"><p>No, never. n8n stores credentials separately from workflow definitions precisely so that workflows can be shared and version-controlled safely. Always leave credential fields empty in generated JSON and attach the credential in the n8n UI. This also keeps your keys out of your AI chat history and out of your Git repo.</p></div></details> <details class="mmd-faq-item"><summary class="mmd-faq-q">Is self-hosted n8n free?</summary><div class="mmd-faq-a"><p>The n8n Community Edition is free to self-host under a fair-code licence, so your only cost is the server. On a Hostinger VPS that is a few dollars a month for unlimited workflow executions, which is why it works out far cheaper than a metered cloud plan once you are running real automations daily.</p></div></details> <details class="mmd-faq-item"><summary class="mmd-faq-q">Can I claim SkillsFuture Credit for these courses?</summary><div class="mmd-faq-a"><p>Yes. Our non-WSQ AI courses, including the Claude Code and Codex masterclasses, are SkillsFuture Credit claimable for eligible Singaporeans. The WSQ n8n and Claude Code courses additionally carry up to 70% SSG course-fee funding, with Absentee Payroll available to employers sponsoring their staff. Eligibility is shown on each course page.</p></div></details> <details class="mmd-faq-item"><summary class="mmd-faq-q">Do I need to know how to code to use this method?</summary><div class="mmd-faq-a"><p>No. You are writing an English description of the automation, not code — the agent produces the JSON. What you do need is the ability to read an error message and hand it back to the agent, and the discipline to test each workflow before trusting it. Both are taught in class from a standing start.</p></div></details> </div> <h2>Next steps</h2> <ol> <li><a href="https://www.hostinger.com?REFERRALCODE=FEGANGCHQ20C" target="_blank" rel="noopener sponsored">Spin up a Hostinger VPS at 20% off</a> and install n8n with the one-click template</li> <li>Export one working workflow as your format reference</li> <li>Generate your next workflow with the prompt template above instead of dragging nodes</li> <li><a href="https://www.tertiarycourses.com.sg/claude-ai-series.html">Book a Claude AI Series course</a> or <a href="https://www.tertiarycourses.com.sg/codex-ai-series.html">a Codex AI Series course</a> and build it properly — with SkillsFuture Credit or up to 70% WSQ funding</li> </ol> <p><em>Disclosure: the Hostinger links in this article are referral links — using them costs you nothing extra and gets you 20% off, while supporting our free tutorials. Tooling in this space moves fast; check the official Claude Code, Codex and n8n documentation for the latest commands.</em></p>', 'Tertiary Infotech Academy', 1, NOW(), 'C1417, C989, C427, TGS-2025052468, TGS-2023035977, C735', 'Build n8n Workflows with Claude Code or Codex (2026 Guide)', 'Skip the node-dragging: generate complete n8n workflows as JSON with Claude Code or OpenAI Codex, paste them onto the canvas, and self-host n8n on a cheap Hostinger VPS. Prompt template, fixes and funded courses included.', 'n8n claude code, n8n codex, build n8n workflow with ai, n8n workflow json, generate n8n workflow, claude code n8n, openai codex n8n, n8n automation course singapore, install n8n hostinger, self host n8n, skillsfuture ai course, wsq n8n course', NOW(), NOW() FROM DUAL WHERE @is_sg > 0 AND NOT EXISTS (SELECT 1 FROM `mmd_blog_post` WHERE `url_key` = 'build-n8n-workflows-with-claude-code-codex');

-- Tags (core Magento tag vocabulary; status 1 = approved)
INSERT INTO `tag` (`name`, `status`) SELECT 'n8n', 1 FROM DUAL WHERE NOT EXISTS (SELECT 1 FROM `tag` WHERE `name` = 'n8n');
INSERT INTO `tag` (`name`, `status`) SELECT 'Automation', 1 FROM DUAL WHERE NOT EXISTS (SELECT 1 FROM `tag` WHERE `name` = 'Automation');
INSERT INTO `tag` (`name`, `status`) SELECT 'Agentic AI', 1 FROM DUAL WHERE NOT EXISTS (SELECT 1 FROM `tag` WHERE `name` = 'Agentic AI');
INSERT INTO `tag` (`name`, `status`) SELECT 'Claude Code', 1 FROM DUAL WHERE NOT EXISTS (SELECT 1 FROM `tag` WHERE `name` = 'Claude Code');
INSERT INTO `tag` (`name`, `status`) SELECT 'Codex', 1 FROM DUAL WHERE NOT EXISTS (SELECT 1 FROM `tag` WHERE `name` = 'Codex');

SET @post_id := (SELECT `post_id` FROM `mmd_blog_post` WHERE `url_key` = 'build-n8n-workflows-with-claude-code-codex');

INSERT IGNORE INTO `mmd_blog_post_tag` (`post_id`, `tag_id`) SELECT @post_id, `tag_id` FROM `tag` WHERE @post_id IS NOT NULL AND `name` IN ('n8n', 'Automation', 'Agentic AI', 'Claude Code', 'Codex');

-- Seed the like count so the post does not ship showing "0 Likes" (same rationale and
-- `likes < 100` idempotency guard as migrations 868/869 — only ever raises into the band,
-- never overwrites a genuine count that has already grown past it).
UPDATE `mmd_blog_post` SET `likes` = 147 WHERE `url_key` = 'build-n8n-workflows-with-claude-code-codex' AND `likes` < 100;
