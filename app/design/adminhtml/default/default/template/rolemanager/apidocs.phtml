<?php
/**
 * Super-Admin API Documentation page.
 *
 * Self-contained: inline <style> is scoped under #api-docs to avoid leaking
 * into other admin pages (see MEMORY: feedback_rolemanager_css_global_bleed).
 * Add new sections per API as <section class="api-section"> blocks.
 */
$apiKey       = (string) Mage::getStoreConfig('courses/general/wsq_schedule_api_key');
$prodBaseUrl  = 'https://www.tertiarycourses.com.sg';
$localBaseUrl = rtrim(Mage::getBaseUrl(Mage_Core_Model_Store::URL_TYPE_LINK), '/');
$apiKeyMasked = $apiKey !== '' ? substr($apiKey, 0, 6) . str_repeat('•', 12) . substr($apiKey, -4) : '(not set)';
?>
<style>
#api-docs {
    color: #e8eaed;
    font-size: 13px;
    line-height: 1.55;
    padding: 0 4px 40px;
    max-width: 1100px;
}
#api-docs h1 {
    font-size: 22px;
    font-weight: 600;
    color: #fff;
    margin: 0 0 4px;
}
#api-docs .page-lede {
    color: #9aa0a6;
    margin: 0 0 24px;
}
#api-docs .api-section {
    background: #1f2227;
    border: 1px solid #2d3138;
    border-radius: 8px;
    padding: 22px 26px;
    margin-bottom: 22px;
}
#api-docs .api-section header {
    display: flex;
    align-items: center;
    gap: 10px;
    border-bottom: 1px solid #2d3138;
    padding-bottom: 14px;
    margin-bottom: 18px;
}
#api-docs .api-section h2 {
    font-size: 18px;
    font-weight: 600;
    color: #fff;
    margin: 0;
    flex: 1;
}
#api-docs .api-pill {
    display: inline-block;
    font-size: 11px;
    padding: 3px 9px;
    border-radius: 10px;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.04em;
}
#api-docs .api-pill.method-get   { background: #1b3a2d; color: #6ee7a7; border: 1px solid #2c5b46; }
#api-docs .api-pill.scope-public { background: #2a2f3c; color: #8ab4f8; border: 1px solid #3a4256; }
#api-docs .api-pill.scope-internal { background: #3a2a2a; color: #f6a5a5; border: 1px solid #5b3a3a; }
#api-docs h3 {
    font-size: 13px;
    text-transform: uppercase;
    letter-spacing: 0.06em;
    color: #9aa0a6;
    margin: 22px 0 10px;
    font-weight: 600;
}
#api-docs h3:first-of-type { margin-top: 0; }
#api-docs p { margin: 0 0 12px; }
#api-docs ul { margin: 0 0 12px 22px; padding: 0; }
#api-docs li { margin-bottom: 4px; }
#api-docs code,
#api-docs pre {
    font-family: 'SFMono-Regular', 'Menlo', 'Consolas', monospace;
}
#api-docs code {
    background: #2a2e35;
    color: #f3c97a;
    padding: 2px 6px;
    border-radius: 4px;
    font-size: 12px;
    border: 1px solid #353a43;
}
#api-docs pre {
    background: #15171b;
    border: 1px solid #2d3138;
    border-radius: 6px;
    padding: 14px 16px;
    font-size: 12px;
    line-height: 1.5;
    overflow-x: auto;
    color: #cfd2d6;
    margin: 0 0 16px;
}
#api-docs pre.url-block { color: #8ab4f8; }
#api-docs table.api-table {
    width: 100%;
    border-collapse: collapse;
    margin: 0 0 18px;
    background: #15171b;
    border: 1px solid #2d3138;
    border-radius: 6px;
    overflow: hidden;
}
#api-docs table.api-table th,
#api-docs table.api-table td {
    text-align: left;
    padding: 9px 14px;
    border-bottom: 1px solid #2d3138;
    vertical-align: top;
}
#api-docs table.api-table thead th {
    background: #1f2227;
    color: #9aa0a6;
    font-size: 11px;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    font-weight: 600;
}
#api-docs table.api-table tbody tr:last-child td { border-bottom: 0; }
#api-docs table.api-table td code { white-space: nowrap; }
#api-docs .status-row td:first-child code { color: #6ee7a7; }
#api-docs .key-box {
    display: flex;
    align-items: center;
    gap: 10px;
    background: #15171b;
    border: 1px solid #2d3138;
    border-radius: 6px;
    padding: 10px 14px;
    margin: 0 0 16px;
}
#api-docs .key-box code {
    flex: 1;
    background: transparent;
    border: 0;
    color: #f3c97a;
    padding: 0;
    font-size: 13px;
    user-select: all;
}
#api-docs .key-box button {
    background: #2a2f3c;
    color: #8ab4f8;
    border: 1px solid #3a4256;
    padding: 5px 12px;
    border-radius: 4px;
    font-size: 12px;
    cursor: pointer;
}
#api-docs .key-box button:hover { background: #34394a; }
#api-docs .key-box .reveal-toggle {
    background: transparent;
    border: 0;
    color: #9aa0a6;
    cursor: pointer;
    font-size: 12px;
    padding: 0 4px;
}
#api-docs .note {
    background: #1a2330;
    border-left: 3px solid #4285f4;
    border-radius: 4px;
    padding: 10px 14px;
    margin: 0 0 14px;
    color: #cfd2d6;
}
#api-docs .note strong { color: #8ab4f8; }
#api-docs .warn {
    background: #2a1f1f;
    border-left: 3px solid #ef6e6e;
    border-radius: 4px;
    padding: 10px 14px;
    margin: 0 0 14px;
    color: #f0d6d6;
}
#api-docs .warn strong { color: #f6a5a5; }
</style>

<div id="api-docs">
    <h1>API Documentation</h1>
    <p class="page-lede">Live integration specs for every public/internal API this LMS exposes. Visible to Super Admin only. Hand this page (or a copy of the relevant section) to integration partners.</p>

    <!-- =========================================================== -->
    <!-- WSQ Schedule API                                             -->
    <!-- =========================================================== -->
    <section class="api-section" id="wsq-schedule">
        <header>
            <h2>WSQ Schedule API</h2>
            <span class="api-pill method-get">GET</span>
            <span class="api-pill scope-public">Public · API-key gated</span>
        </header>

        <p>Read-only endpoint listing every WSQ course (<code>TGS-</code> course code prefix) enabled in the Singapore store, with its scheduled class dates. Consumed by the external TMS to keep its course catalog in sync.</p>

        <h3>Endpoint</h3>
        <pre class="url-block">GET <?php echo $this->htmlEscape($prodBaseUrl); ?>/courses/api_schedule</pre>
        <p style="color:#9aa0a6;font-size:12px;margin-top:-8px;">Localhost equivalent: <code><?php echo $this->htmlEscape($localBaseUrl); ?>/courses/api_schedule</code></p>

        <h3>Authentication</h3>
        <p>Send the shared secret on every request as an HTTP header:</p>
        <pre>X-API-Key: &lt;your key&gt;</pre>

        <p><strong>Current key</strong> <span style="color:#9aa0a6;">— rotate via System → Configuration → MMD → Provider Manager → WSQ Schedule API Key. Setting it blank disables the endpoint (503).</span></p>
        <div class="key-box">
            <code id="apikey-value" data-revealed="0" data-full="<?php echo $this->htmlEscape($apiKey); ?>"><?php echo $this->htmlEscape($apiKeyMasked); ?></code>
            <button type="button" class="reveal-toggle" onclick="apiDocsToggleKey()">Reveal</button>
            <button type="button" onclick="apiDocsCopyKey()">Copy</button>
        </div>

        <h3>Status codes</h3>
        <table class="api-table">
            <thead><tr><th style="width:90px;">Status</th><th>Meaning</th></tr></thead>
            <tbody>
                <tr class="status-row"><td><code>200</code></td><td>Authenticated; payload returned</td></tr>
                <tr><td><code>401</code></td><td>Missing or wrong <code>X-API-Key</code> → <code>{"error":"unauthorized"}</code></td></tr>
                <tr><td><code>500</code></td><td>Server error → <code>{"error":"internal_error"}</code>. Check <code>var/log/exception.log</code> on the LMS host.</td></tr>
                <tr><td><code>503</code></td><td>Endpoint disabled (server-side key is blank) → <code>{"error":"api_disabled"}</code></td></tr>
            </tbody>
        </table>

        <h3>Scope</h3>
        <ul>
            <li><strong>Enabled products only</strong> (<code>status = 1</code>) — disabled / discontinued courses are excluded.</li>
            <li>Course code starts with <code>TGS-</code> (the SkillsFuture course reference — defines "WSQ" per the LMS data model).</li>
            <li>Singapore store view only (<code>store_id = 1</code>); titles and option labels are returned in the SG storefront's text.</li>
        </ul>

        <h3>Response</h3>
        <p><code>Content-Type: application/json; charset=utf-8</code> · <code>Cache-Control: no-store</code> (always fresh; do not edge-cache).</p>

        <p><strong>Top-level shape</strong></p>
        <pre>{
  "generated_at": "2026-05-31T05:45:28+00:00",
  "store": "singapore",
  "count": 299,
  "courses": [ ... ]
}</pre>
        <table class="api-table">
            <thead><tr><th>Field</th><th>Type</th><th>Notes</th></tr></thead>
            <tbody>
                <tr><td><code>generated_at</code></td><td>string (ISO 8601, UTC)</td><td>Server-side timestamp at response build</td></tr>
                <tr><td><code>store</code></td><td>string</td><td>Always <code>"singapore"</code> in v1 (WSQ is SG-only)</td></tr>
                <tr><td><code>count</code></td><td>integer</td><td>Number of courses in <code>courses[]</code></td></tr>
                <tr><td><code>courses</code></td><td>array</td><td>List of course objects (see below)</td></tr>
            </tbody>
        </table>

        <p><strong>Course object</strong></p>
        <pre>{
  "course_code": "TGS-2023021752",
  "course_title": "WSQ - Mastering The Art of Conflict Resolution ...",
  "schedules": [ ... ]
}</pre>
        <table class="api-table">
            <thead><tr><th>Field</th><th>Type</th><th>Notes</th></tr></thead>
            <tbody>
                <tr><td><code>course_code</code></td><td>string</td><td><strong>The course code and the official SkillsFuture course reference.</strong> Always <code>TGS-</code> prefix. Stable primary key — TMS should join on this.</td></tr>
                <tr><td><code>course_title</code></td><td>string</td><td>Course name as shown in the SG storefront.</td></tr>
                <tr><td><code>schedules</code></td><td>array</td><td>May be empty (course exists but has no scheduled dates).</td></tr>
            </tbody>
        </table>

        <p><strong>Schedule object</strong></p>
        <pre>{
  "raw": "29 Jun - 3 Jul 2026 (Mon-Fri)",
  "course_start_date": "2026-06-29",
  "course_end_date": "2026-07-03"
}</pre>
        <table class="api-table">
            <thead><tr><th>Field</th><th>Type</th><th>Notes</th></tr></thead>
            <tbody>
                <tr><td><code>raw</code></td><td>string</td><td>The exact label shown to learners on the storefront's "Course Date" dropdown. <strong>Always present.</strong></td></tr>
                <tr><td><code>course_start_date</code></td><td>string <code>YYYY-MM-DD</code> or <code>null</code></td><td>First class day. <code>null</code> only when the raw label is in an unsupported format (~0.25% of rows).</td></tr>
                <tr><td><code>course_end_date</code></td><td>string <code>YYYY-MM-DD</code> or <code>null</code></td><td>Last class day. Same nullability as start. For a same-day course, start == end.</td></tr>
            </tbody>
        </table>

        <div class="note">
            <strong>Null handling:</strong> if <code>course_start_date</code> is <code>null</code>, fall back to displaying / parsing <code>raw</code>. Do not drop the row — these are real, live class offerings the parser couldn't normalise (typically multi-segment cross-month labels like <code>"23/24/31 May /06/07 Jun 2026"</code>).
        </div>

        <h3>Supported label formats</h3>
        <table class="api-table">
            <thead><tr><th>Pattern</th><th>Example</th><th>Parsed output</th></tr></thead>
            <tbody>
                <tr><td>Single day</td><td><code>20 May 2026</code></td><td>start = end = <code>2026-05-20</code></td></tr>
                <tr><td>Slash, same-month</td><td><code>25/26 May 2026 (Mon/Tue)</code></td><td><code>2026-05-25</code> → <code>2026-05-26</code></td></tr>
                <tr><td>Multi-day slash same-month</td><td><code>7/8/10 Mar 2016</code></td><td>First to last day</td></tr>
                <tr><td>Hyphen range same-month</td><td><code>15-18 Jul 2026</code></td><td><code>2026-07-15</code> → <code>2026-07-18</code></td></tr>
                <tr><td>Cross-month slash</td><td><code>30 Nov / 1 Dec 2023</code></td><td><code>2023-11-30</code> → <code>2023-12-01</code></td></tr>
                <tr><td>Cross-month slash compact</td><td><code>30/1 June/July 2018</code></td><td><code>2018-06-30</code> → <code>2018-07-01</code></td></tr>
                <tr><td>Cross-month hyphen</td><td><code>29 Jun - 3 Jul 2026</code></td><td><code>2026-06-29</code> → <code>2026-07-03</code></td></tr>
                <tr><td>Dot / ampersand separators</td><td><code>21.22 Feb 2019</code>, <code>4 &amp; 11 July 2015</code></td><td>First to last day</td></tr>
                <tr><td>ISO</td><td><code>2026-05-20</code></td><td>Pass-through</td></tr>
                <tr><td>With bracket noise</td><td><code>08 Nov 2021 (Mon) [Zoom only]</code></td><td>Annotations stripped before parsing</td></tr>
            </tbody>
        </table>

        <h3>Caching &amp; polling</h3>
        <ul>
            <li>Server sends <code>Cache-Control: no-store</code>. Each call recomputes from the live DB.</li>
            <li>Payload is ~440 KB for the current catalog (299 courses, ~4,800 schedule rows). Poll every 5–15 minutes.</li>
            <li>No webhook in v1 — TMS polls. The payload changes only when admin staff edit course schedules.</li>
        </ul>

        <h3>Stability &amp; versioning</h3>
        <ul>
            <li><code>course_code</code> is the join key. It is permanent; never reused for a different course.</li>
            <li>Field names are stable. Future additive fields (e.g. <code>trainer_name</code>, <code>course_mode</code>) will be added without breaking existing consumers — <strong>TMS must ignore unknown fields</strong>.</li>
            <li>Incompatible changes would land at <code>/courses/api_schedule/v2</code>; v1 stays for a deprecation window.</li>
        </ul>

        <h3>Smoke test</h3>
        <pre>curl -sS \
  -H "X-API-Key: <span style="color:#f3c97a;">&lt;your key&gt;</span>" \
  <?php echo $this->htmlEscape($prodBaseUrl); ?>/courses/api_schedule \
  | jq '{count, sample: .courses[0]}'</pre>

        <p><strong>Expected output</strong></p>
        <pre>{
  "count": 299,
  "sample": {
    "course_code": "TGS-2023021752",
    "course_title": "WSQ - Mastering The Art of Conflict Resolution ...",
    "schedules": [
      {
        "raw": "25/26 May 2026 (Mon/Tue)",
        "course_start_date": "2026-05-25",
        "course_end_date": "2026-05-26"
      }
    ]
  }
}</pre>

        <div class="warn">
            <strong>Rotation:</strong> when rotating the API key, update the TMS configuration before rotating here — otherwise the TMS will start receiving 401s. There is no grace period and no key history.
        </div>
    </section>

    <!-- Add new <section class="api-section"> blocks below as more APIs ship. -->

    <!-- =========================================================== -->
    <!-- WhatsApp Bot APIs (5 endpoints, same X-API-Key)              -->
    <!-- =========================================================== -->

    <section class="api-section" id="bot-search">
        <header>
            <h2>Course Search API</h2>
            <span class="api-pill method-get">GET</span>
            <span class="api-pill scope-public">Public · API-key gated</span>
        </header>
        <p>Keyword search across SG storefront courses (any course-code prefix). Used by the WhatsApp bot to answer <em>"do you have a Python course?"</em> style questions.</p>

        <h3>Endpoint</h3>
        <pre class="url-block">GET <?php echo $this->htmlEscape($prodBaseUrl); ?>/courses/api_search?q=&lt;keyword&gt;&amp;limit=10</pre>

        <h3>Query parameters</h3>
        <table class="api-table">
            <thead><tr><th>Param</th><th>Required</th><th>Notes</th></tr></thead>
            <tbody>
                <tr><td><code>q</code></td><td>Yes</td><td>Keyword matched against <code>name</code> OR <code>sku</code> (LIKE %q%, case-insensitive).</td></tr>
                <tr><td><code>limit</code></td><td>No</td><td>1-50 (default 10). Higher values are clamped to 50.</td></tr>
            </tbody>
        </table>

        <h3>Authentication</h3>
        <p>Same <code>X-API-Key</code> header as the WSQ Schedule API above.</p>

        <h3>Response shape</h3>
        <pre>{
  "source_url": "https://www.tertiarycourses.com.sg/catalogsearch/result/?q=python",
  "last_updated": "2026-06-05T04:00:00+00:00",
  "confidence": "high",       // "low" when count = 0
  "data": {
    "query": "python",
    "limit": 10,
    "count": 3,
    "results": [
      {
        "sku": "C138",
        "name": "Python 3 Essential Training",
        "url": "https://www.tertiarycourses.com.sg/python-3-essential-training-in-singapore.html",
        "image_url": "https://r2.../small_image/400x/.../python.jpg",
        "price": "S$600.00",
        "short_description": "Delve deep into the world of Python 3...",
        "funding_badges": ["WSQ", "SkillsFuture Credit"]
      }
    ]
  }
}</pre>

        <h3>Sample curl</h3>
        <pre>curl -H "X-API-Key: &lt;key&gt;" \
  "<?php echo $this->htmlEscape($prodBaseUrl); ?>/courses/api_search?q=python&amp;limit=5"</pre>
    </section>

    <section class="api-section" id="bot-course-detail">
        <header>
            <h2>Course Detail API</h2>
            <span class="api-pill method-get">GET</span>
            <span class="api-pill scope-public">Public · API-key gated</span>
        </header>
        <p>Full details for one course by course code — overview, suitability, prerequisites, duration, level, assessment, certification, fee, funding badges, mode, image, course page URL, and registration link.</p>

        <h3>Endpoint</h3>
        <pre class="url-block">GET <?php echo $this->htmlEscape($prodBaseUrl); ?>/courses/api_courses?sku=&lt;course_code&gt;</pre>

        <h3>Query parameters</h3>
        <table class="api-table">
            <thead><tr><th>Param</th><th>Required</th><th>Notes</th></tr></thead>
            <tbody>
                <tr><td><code>sku</code></td><td>Yes</td><td>Course code (e.g. <code>C138</code>, <code>TGS-2023021752</code>). Returns 404 if not found.</td></tr>
            </tbody>
        </table>

        <h3>Response shape (truncated)</h3>
        <pre>{
  "source_url": "https://www.tertiarycourses.com.sg/python-3-essential-training-in-singapore.html",
  "last_updated": "...",
  "confidence": "high",
  "data": {
    "sku": "C138",
    "course_code": "C138",
    "name": "Python 3 Essential Training",
    "overview": "Delve deep into the world of Python 3...",
    "description_full": "Topic 1 Get Started with Python...",
    "suitability": "...",
    "prerequisites": "Knowledge and Skills: Able to operate using computer functions...",
    "duration": "15",
    "level": "Beginner to Intermediate",
    "assessment": "",
    "certification": "Tertiary Infotech Certificate of Completion",
    "fee": { "list_price": "S$600.00", "list_price_raw": 600, "currency": "SGD", "note": "..." },
    "venue": "Tertiary Infotech HQ...",
    "mode": "Classroom and/or Live Online",
    "funding_badges": ["WSQ", "SkillsFuture Credit"],
    "image_url": "...",
    "course_page_url": "...",
    "registration_url": ".../#schedule"
  }
}</pre>

        <h3>Sample curl</h3>
        <pre>curl -H "X-API-Key: &lt;key&gt;" \
  "<?php echo $this->htmlEscape($prodBaseUrl); ?>/courses/api_courses?sku=C138"</pre>
    </section>

    <section class="api-section" id="bot-schedule-sku">
        <header>
            <h2>Schedule by Course Code (extension of WSQ Schedule)</h2>
            <span class="api-pill method-get">GET</span>
            <span class="api-pill scope-public">Public · API-key gated</span>
        </header>
        <p>Per-course schedule lookup with trainer name, vacancy, mode, and status — extends the existing <code>/courses/api_schedule</code> endpoint. Same URL; adding <code>?sku=</code> switches into the per-course mode.</p>

        <div class="note"><strong>Backwards compatible.</strong> Calling <code>/courses/api_schedule</code> with NO query params still returns the original WSQ-bulk shape — existing consumers are unaffected.</div>

        <h3>Endpoint</h3>
        <pre class="url-block">GET <?php echo $this->htmlEscape($prodBaseUrl); ?>/courses/api_schedule?sku=&lt;course_code&gt;</pre>

        <h3>Response shape</h3>
        <pre>{
  "source_url": ".../python-3-essential-training-in-singapore.html",
  "last_updated": "...",
  "confidence": "high",   // "low" if no upcoming classes
  "data": {
    "course_code": "C138",
    "course_title": "Python 3 Essential Training",
    "course_page_url": "...",
    "classes_count": 2,
    "classes": [
      {
        "class_id": "C004267",
        "start_date": "2026-06-11",
        "end_date": "2026-06-12",
        "time": "09:30-17:30",
        "trainer": "Sanjiv Venkatram",   // null if not yet assigned
        "status": "Confirmed",            // "Pending" when trainer is null
        "mode": "Classroom",
        "venue": "Springleaf Tower",
        "vacancy_code": "A",
        "seats_available": "Available"
      }
    ]
  }
}</pre>

        <h3>Sample curl</h3>
        <pre>curl -H "X-API-Key: &lt;key&gt;" \
  "<?php echo $this->htmlEscape($prodBaseUrl); ?>/courses/api_schedule?sku=C138"</pre>
    </section>

    <section class="api-section" id="bot-faq">
        <header>
            <h2>FAQ / Funding / Policy API</h2>
            <span class="api-pill method-get">GET</span>
            <span class="api-pill scope-public">Public · API-key gated</span>
        </header>
        <p>Static knowledge base of SG funding schemes (SkillsFuture, WSQ, UTAP, PSEA, MCES, IBF, SFEC, Absentee Payroll) and policy topics (cancellation, certificates, assessments, corporate registration). Returns ALL topics in one response — the bot does keyword matching locally.</p>

        <div class="note"><strong>Content is editorial.</strong> Edit <code>app/code/local/MMD/Courses/controllers/Api/FaqController.php</code> to add/update topics. Each topic includes a <code>keywords</code> array the bot uses for matching.</div>

        <h3>Endpoint</h3>
        <pre class="url-block">GET <?php echo $this->htmlEscape($prodBaseUrl); ?>/courses/api_faq</pre>

        <h3>Response shape (per topic)</h3>
        <pre>{
  "id": "skillsfuture-credit",
  "title": "SkillsFuture Credit (SFC)",
  "keywords": ["skillsfuture", "sfc", "credit", "$500", "claim", "subsidy"],
  "summary": "Every Singapore Citizen aged 25+ receives a $500 SkillsFuture Credit...",
  "eligibility": "Singapore Citizens aged 25 and above...",
  "how_to_claim": ["Browse approved courses...", "Add to cart...", "..."],
  "official_source": "https://www.skillsfuture.gov.sg/credit"
}</pre>

        <p><strong>Topics currently exposed:</strong> <code>skillsfuture-credit</code>, <code>wsq</code>, <code>utap</code>, <code>psea</code>, <code>mces</code>, <code>ibf</code>, <code>sfec</code>, <code>absentee-payroll</code>, <code>cancellation-policy</code>, <code>certificates</code>, <code>assessments</code>, <code>corporate-registration</code>.</p>

        <h3>Sample curl</h3>
        <pre>curl -H "X-API-Key: &lt;key&gt;" \
  "<?php echo $this->htmlEscape($prodBaseUrl); ?>/courses/api_faq" | jq '.data.topics[] | {id, title}'</pre>
    </section>

    <section class="api-section" id="bot-contact">
        <header>
            <h2>Contact &amp; Office Hours API</h2>
            <span class="api-pill method-get">GET</span>
            <span class="api-pill scope-public">Public · API-key gated</span>
        </header>
        <p>SG office contact (phone, email, WhatsApp, address) plus office hours. Used by the bot to escalate gracefully when it can't answer ("we're closed now — please email…").</p>

        <h3>Endpoint</h3>
        <pre class="url-block">GET <?php echo $this->htmlEscape($prodBaseUrl); ?>/courses/api_contact</pre>

        <h3>Response shape</h3>
        <pre>{
  "source_url": "https://www.tertiarycourses.com.sg/contacts",
  "last_updated": "...",
  "confidence": "medium",
  "data": {
    "office": {
      "name": "Tertiary Infotech Academy — Singapore",
      "address": "3 Anson Road, #12-01 Springleaf Tower, Singapore 079909",
      "phone": "+65 6720 3333",
      "email": "enquiry@tertiaryinfotech.com",
      "whatsapp": "+65 8866 6375"
    },
    "office_hours": {
      "timezone": "Asia/Singapore",
      "monday_friday": "09:00 - 18:00 SGT",
      "saturday": "09:00 - 13:00 SGT",
      "sunday": "Closed",
      "public_holidays": "Closed (Singapore public holidays)"
    },
    "fallback_message": "For urgent enquiries outside office hours...",
    "response_times": { "whatsapp": "...", "email": "...", "phone": "..." }
  }
}</pre>

        <div class="warn"><strong>Editorial content.</strong> Verify the office address, phone, and email in <code>app/code/local/MMD/Courses/controllers/Api/ContactController.php</code> before relying on the bot to give customers your address.</div>

        <h3>Sample curl</h3>
        <pre>curl -H "X-API-Key: &lt;key&gt;" \
  "<?php echo $this->htmlEscape($prodBaseUrl); ?>/courses/api_contact"</pre>
    </section>

</div>

<script>
function apiDocsToggleKey() {
    var el = document.getElementById('apikey-value');
    var btn = el.parentElement.querySelector('.reveal-toggle');
    if (el.getAttribute('data-revealed') === '1') {
        el.textContent = '<?php echo addslashes($apiKeyMasked); ?>';
        el.setAttribute('data-revealed', '0');
        btn.textContent = 'Reveal';
    } else {
        el.textContent = el.getAttribute('data-full');
        el.setAttribute('data-revealed', '1');
        btn.textContent = 'Hide';
    }
}
function apiDocsCopyKey() {
    var el = document.getElementById('apikey-value');
    var full = el.getAttribute('data-full');
    if (!full) return;
    if (navigator.clipboard && navigator.clipboard.writeText) {
        navigator.clipboard.writeText(full).then(function () {
            var btn = el.parentElement.querySelectorAll('button')[1];
            var orig = btn.textContent;
            btn.textContent = 'Copied ✓';
            setTimeout(function () { btn.textContent = orig; }, 1500);
        });
    } else {
        var ta = document.createElement('textarea');
        ta.value = full;
        document.body.appendChild(ta);
        ta.select();
        try { document.execCommand('copy'); } catch (e) {}
        document.body.removeChild(ta);
    }
}
</script>
