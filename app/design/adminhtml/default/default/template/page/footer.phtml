<?php
/**
 * Admin footer - Tertiary Infotech Academy
 */
?>
<?php
$_version = '';
$_versionFile = BP . '/version.txt';
if (file_exists($_versionFile)) {
    $_version = trim(file_get_contents($_versionFile));
}
// Git commit timestamp as version
$_commitVersion = '';
$_commitVersionFile = BP . '/commit-version.txt';
if (file_exists($_commitVersionFile)) {
    $_commitVersion = trim(file_get_contents($_commitVersionFile));
}
?>
<?php $_v = $_commitVersion ? $_commitVersion : $_version; ?>
<div class="admin-bottom-bar">
    <span class="admin-bottom-powered">Powered by <strong>Tertiary Infotech Academy Pte Ltd</strong> &copy; <?php echo date('Y') ?></span>
</div>
<style>
.admin-bottom-bar{
    position:static;
    margin-left:-24px;
    margin-right:0;
    display:flex;
    align-items:center;
    justify-content:center;
    padding:12px 24px;
    background:#111827;
    border-top:1px solid var(--b1);
    z-index:1100;
    transition:left 0.25s ease;
}
body.sidebar-collapsed .admin-bottom-bar{left:60px}
.admin-bottom-powered{color:var(--t5);font-size:13px}
.admin-bottom-powered strong{color:var(--t2)}
</style>
